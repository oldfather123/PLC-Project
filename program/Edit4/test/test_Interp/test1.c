int main() {
    int a = 10;
    int b = 20;
    int sum = a + b;
    putint(sum);
    
    int diff = b - a;
    putint(diff);
    
    int product = a * 3;
    putint(product);
    
    int quotient = b / a;
    putint(quotient);
    
    int remainder = b % 3;
    putint(remainder);
    
    return 0;
}